{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/auth0-todo-api.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}